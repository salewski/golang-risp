(namespace fibbonaci)

(defun fib (n) (case n
	(0) 0
	(1) 1
	_   (+ (fibbonaci:fib (- n 1)) (fibbonaci:fib (- n 2)))
))

(for (range 0 21) (i) (
	(print (cat "Fibbonaci Sequence #" i ": "))
	(println (fibbonaci:fib i))
))

(export fibbonaci:fib)